/**
 * Team-supplied content for the /dive story. Sections that depend on these lists
 * stay hidden while they are empty, so the page is always safe to share.
 */

export const TEAM: string[] = ["Theodore", "Issaka", "Oliver"];

/** Hackathon tracks, each with one sentence on how TIDE fits. */
export const TRACKS: { name: string; fit: string }[] = [];

/** Impact numbers exactly as the team states them. `projected` labels forecasts as such. */
export const IMPACT: { value: string; label: string; projected?: boolean }[] = [];

export const REPO_URL = "https://github.com/theokkk4/TIDE";
